extends ../layout

block content
  h1 Add Movie Form
  if errors.length
    ul
      each e in errors
        li= e

  form(method="POST" action="/movies/add")
    div
      label Name
      input(type="text" name="name" value=form.name || "")
    div
      label Description
      textarea(name="description")= form.description || ""
    div
      label Year
      input(type="number" name="year" value=form.year || "")
    div
      label Genres
      input(type="text" name="genres" value=form.genres || "")
    div
      label Rating (0-10)
      input(type="number" step="0.1" name="rating" value=form.rating || "")
    button(type="submit") Save